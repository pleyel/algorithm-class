# 계단 오르는 방법 수 계산 (Bottom-up DP)
n = int(input("계단의 개수를 입력하시오: "))

# n이 0 이상이라는 전제
if n == 0:
    print("0개의 계단을 오르는 방법의 수는 1가지입니다.")
else:
    # DP 테이블 생성
    dp = [0] * (n + 1)

    # 초기값 설정
    dp[1] = 1
    if n >= 2:
        dp[2] = 2

    # Bottom-up 방식으로 테이블 채우기
    for i in range(3, n + 1):
        dp[i] = dp[i - 1] + dp[i - 2]

    # 결과 출력
    print(f"{n}개의 계단을 오르는 방법의 수는 {dp[n]}가지입니다.")
